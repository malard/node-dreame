node_modules/
dist/
*.log
.env
.env.local
.DS_Store
coverage/
.vitest-cache/
.tsbuildinfo
.research/
.claude/
*.apk

# Live-map fixtures contain raw OSS object names + per-blob AES keys —
# keep out of the repo until a sanitization pass exists.
test/fixtures/map/

# Probe-script output captures carry the active did / uid / OSS
# object names from a real session. Never committed.
probe-*.jsonl
threshold-*.jsonl
